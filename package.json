{
  "name": "twelvepoints",
  "version": "1.0.0",
  "private": true,
  "description": "Display a year as a circle of twelve points marked yes/no per month",
  "scripts": {
    "test": "node --test checks/format.test.mjs"
  },
  "devDependencies": {
    "jsdom": "^24.0.0"
  }
}
